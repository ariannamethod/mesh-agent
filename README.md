# mesh-agent — moved

**This repository is archived.** `mesh-agent` is now a canonical
component of **metaharmonix**:

→ https://github.com/ariannamethod/metaharmonix — `bake/mesh-agent/`

All development since v0.1.0 — the `mhx mesh` builtin, the
UserPromptSubmit inbox hook, and the bind / node-name / Android-netlink
fixes — happened there. This repo is a frozen v0.1.0 snapshot kept for
history; it does not receive updates.

— Arianna Method, 2026-05-17
